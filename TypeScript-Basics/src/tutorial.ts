// Union Type
let tax: number | string = 0.1

let requestStatus: "success" | "error" | "loading" = "success"

// Type Any
let anyValue: any = "this is a string"

// ## Challenge
let orderStatus: "pending" | "completed" | "cancelled"
orderStatus = "pending"
